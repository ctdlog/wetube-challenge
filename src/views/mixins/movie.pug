mixin movie(movie)
    div
        h4
            a(href=`/movies/${movie.id}`)=movie.title
        p=movie.summary
        small #{movie.year}년 / #{movie.rating}점
        li=movie.genres
        hr
        
        